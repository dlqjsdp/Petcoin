import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './admin/components/AdminDashboard';  //경로수정

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);